# Main pipeline - runs on main branch push

name: Main Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.version.outputs.version }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Calculate semantic version
        uses: PaulHatch/semantic-version@v5.2.0
        with:
          prefix: 'v'
        id: version

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build and test
        env:
          APP_VERSION: ${{ steps.version.outputs.version }}
        run: |
          ./gradlew build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: build/libs/*.jar

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    environment:
      name: Prod
      url: https://notifier.vachoo.ru

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: app
          path: build/libs/

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        env:
          APP_VERSION: ${{ needs.build.outputs.VERSION }}
        run: |
          ./gradlew jib --image=ghcr.io/khachaturb/notifier-service:$APP_VERSION

      - name: Set up kubectl
        uses: azure/k8s-set-context@v4
        with:
          kubeconfig: ${{ secrets.KUBECONFIG }}

      - name: Create image pull secret
        run: |
          kubectl create secret docker-registry ghcr-secret \
            --docker-server=ghcr.io \
            --docker-username=${{ github.actor }} \
            --docker-password=${{ secrets.IMAGE_PULL_SECRET }} \
            --namespace=app \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy to Kubernetes
        env:
          NIM_API_KEY: ${{ secrets.NIM_API_KEY }}
        run: |
          # Create secret for NIM_API_KEY
          kubectl create secret generic notifier-secrets \
            --namespace=app \
            --from-literal=nim-api-key=$NIM_API_KEY \
            --dry-run=client -o yaml | kubectl apply -f -
          
          # Apply k8s manifests
          kubectl apply -f k8s/ --namespace=app
          
          # Patch the NIM_API_KEY env var in deployment
          kubectl set env deployment/notifier-service NIM_API_KEY=$NIM_API_KEY --namespace=app

      - name: Wait for deployment
        run: |
          kubectl rollout status deployment/notifier-service --namespace=app --timeout=300s

      - name: Create git tag
        run: |
          git tag "v${{ needs.build.outputs.VERSION }}"
          git push origin "v${{ needs.build.outputs.VERSION }}"

      - name: Create GitHub release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ needs.build.outputs.VERSION }}
          release_name: Release v${{ needs.build.outputs.VERSION }}
          draft: false
          prerelease: false
